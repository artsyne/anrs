# ANRS Development Dependencies
#
# Note: ANRS is a specification framework, not a production application.
# These dependencies are for running examples and development tooling only.

# For running harness examples
pytest>=7.0.0

# For JSON schema validation (optional)
jsonschema>=4.0.0

# For development
pyyaml>=6.0.0
